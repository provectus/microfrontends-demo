import React from 'react';

export default () => (
  <div className="section">
    <h1 className="title">Products App.</h1>
    <h2 className="subtitle">It uses the old REACT version (just for fun)</h2>
  </div>
);
